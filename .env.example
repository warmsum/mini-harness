# Copy this tracked template to .env. Demos read DEEPSEEK_API_KEY from it;
# never put a real key in this file.
# 请把这个模板复制为 .env，填入真实 Key。不要把真实 Key 写进本文件。
#
# mini-harness 的 17 个章节只读取这一个变量（.env 未被 git 跟踪）；
# 教学代码中的模型和端点直接写在对应章节里，不读取其他配置项。
# 如果运行官方 DSH，DEEPSEEK_BASE_URL、DSH_MODEL 等启动级变量应通过
# 进程环境传入；官方启动器会拒绝从 .env 读取它们。
DEEPSEEK_API_KEY=your_deepseek_api_key_here
